"""LangChain-tool wrappers around the domain/engine layer (§4.5). Each tool
is a plain, typed, independently unit-testable function first; the LangChain
`@tool` wrapping is a thin layer on top, not where any logic lives."""
