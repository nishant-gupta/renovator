"""Deep Agent orchestrator + sub-agents (design doc §4.6), built with
`deepagents.create_deep_agent`. Assembled in Phase 3 (single-agent MVP) and
Phase 4 (sub-agent delegation) of the implementation plan."""
